<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ba.sum.fpmoz.dnevnik">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dnevnik">
        <activity android:name=".TeacherMainActivity"></activity>
        <activity android:name=".StudentMainActivity" />
        <activity android:name=".TabbedCurseAdminActivity" />
        <activity android:name=".TeacherEditAdminActivity" />
        <activity android:name=".TabbedClassAdminActivity" />
        <activity android:name=".UserEditAdminActivity" />
        <activity android:name=".TabbedUserAdminActivity" />
        <activity android:name=".HomeNavigationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>